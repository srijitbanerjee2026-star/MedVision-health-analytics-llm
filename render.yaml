services:
  - type: web
    name: medvision-guard-backend
    runtime: python
    rootDir: userweb
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: ALLOWED_ORIGINS
        sync: false
